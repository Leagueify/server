{
  "name": "leagueify-server",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist && echo 'Done.'",
    "dev": "bun run --hot index.ts",
    "lint": "prettier --check . && eslint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "bun-types": "latest",
    "eslint": "^8.53.0",
    "eslint-config-prettier": "^9.0.0",
    "prettier": "^3.1.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@stoplight/elements": "^7.13.11",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.4",
    "swagger-autogen": "^2.23.7"
  }
}
